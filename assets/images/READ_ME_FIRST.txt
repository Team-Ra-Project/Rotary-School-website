These folders are intentionally empty in the delivered zip.

Run this once, from the project root (same folder as index.html):

    python3 build/download_images.py

It downloads every photo the site uses from the old live site
(rotaryschooluran.com) and saves it here, so the new site never hotlinks
images from the old one. See the main README.md for details.
