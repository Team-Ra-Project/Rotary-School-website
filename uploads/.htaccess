# Uploaded files must never be executable, no matter what extension a file ends up with.
<FilesMatch "\.(php|phtml|php\d|phar|cgi|pl|py|sh)$">
  Require all denied
</FilesMatch>

Options -ExecCGI
php_flag engine off
